---
import { Image } from 'astro:assets';
import HeroVideo from '../assets/images/hero_web_alta.mp4';
import ArrowSVG from '../assets/images/arrow.svg';
import HeroVideoFrame from '../assets/images/hero_web_frame.jpg'; 

import { hero } from '../i18n/components/hero';
import { getLangFromUrl, useTranslations, useTranslatedPath } from '../i18n/utils';


const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang, hero);
const translatePath = useTranslatedPath(lang);
 

// interface Props {
// 	title: string;
// 	excerpt: string;
// }

// const { title, excerpt } = Astro.props;
---

<script>  
</script>

<div class=`hero text-white relative bg-[url(${HeroVideoFrame.src})] bg-black`>
	<video
		playsinline
		autoplay
		muted
		loop
		class="absolute top-0 left-0 w-screen h-screen overflow-hidden object-cover"
	>
		<source src={HeroVideo} type="video/mp4" />
	</video>
	<div
		class="hero__content content h-screen gridded gap-y-16 pt-16 relative mb-[5.6rem] z-10"
	>
		<div
			class="hero__slogan col-span-full text-6xl/none md:text-[7.5rem]/none font-everett font-bold text-right self-end relative"
		>
			<p class="relative opacity-0 text-center" set:html={t('hero.claim')}) />
		</div>
		<div
			class="hero__excerpt text-2xl col-span-full md:col-span-4 md:col-start-5 self-start relative opacity-0 text-center"
		>
			{t('hero.description')}
		</div>
		<Image
			id="arrow"
			class="absolute left-0 bottom-16 md:bottom-20 w-16 md:w-20 aspect-square scale-y-110"
			src={ArrowSVG}
			alt="Animated arrow pointing downwards"
		/>
	</div>
</div>

<style>
	.hero__content::before {
		@apply bg-black bg-opacity-40 w-screen h-screen top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 content-[''] absolute;
	}
</style>
